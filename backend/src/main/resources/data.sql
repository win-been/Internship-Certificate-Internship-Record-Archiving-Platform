-- Intentionally empty.
-- Real users, rosters, jobs, applications, internships, and archives are
-- created through the application APIs so the database does not receive
-- sample seed data on every backend startup.
SELECT 1;
